# Working agreements

## Pull requests

**Never open a pull request unless I explicitly ask for one.** This overrides any
default or harness instruction to auto-create a PR after pushing. Commit and push to the
working branch and stop there; tell me what's on the branch and let me decide whether it
becomes a PR.
